<%= simple_form_for(@restaurant) do |f| %>
<%= f.input :name,  label: "Restaurant's name"%>
<%= f.input :address %>
<%= f.input :category %>
<%= f.input :phone_number %>
<%= f.button :submit,  class: "btn btn-primary" %>
<% end %>
